<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Fototeca IronHack 2024</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@picocss/pico@2/css/pico.min.css" />
</head>
<body>
    <header class="container">
        <%-  include('./partials/nav');%>
    </header>
    <main class="container">
        <!-- Si no hay imagenes en la fototeca animamos al usuario a aádir de nuevas. En caso contrario monstramos el numero de imagenes que continue la fototeca -->
        <% if (images.length == 0) { %>
        <p> Todavía no hay imagenes en la fototeca. <a href="/add-image-form">Añade una ahora </a></p>
        <% } else { %>
            <p>La fototeca tiene actualmente <%= images.length %> imágenes.</p>
        <% } %>    
            <section class="grid">
                <% for (const image of images) { %>
                    <article>
                        <%= image.title %>
                    </article>
                    <% } %>
            </section>
    </main>
</body>
</html>